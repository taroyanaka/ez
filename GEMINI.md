# GEMINI.md (Project Specifications)

このファイルは、コーディングエージェント（Antigravity等）が当プロジェクトを効率的に把握し、適切なコーディングやファイル操作を行うための仕様書です。
エージェントはコードの生成・修正を行う際、必ずこの仕様を遵守してください。

## 1. プロジェクト概要 (Project Overview)
本プロジェクトは、主にフロントエンド技術（HTML/JS/CSS）を用いたWebアプリケーションまたはゲーム開発のリポジトリです。
エージェントは各タスクの目的に合わせて、既存の構成に沿ったコーディングを行ってください。

## 2. 技術スタック (Technology Stack)
* **コア技術**: HTML, Vanilla JavaScript, Vanilla CSS
* **ビルド/バンドルツール**: 特定のビルドツール（Webpack, Vite等）の明確な指定がない場合は、原則としてバンドラを使用しないプレーンなJS環境を前提として動作するコードを作成してください。
* **デザイン/スタイリング**: TailwindCSS等のフレームワークは使用せず、Vanilla CSS（`game.css`など）を使用してください。ユーザー体験（UX）とモダンなデザイン（UI）を意識したスタイリングを行ってください。

## 3. ディレクトリ構造 (Directory Structure)
現在の主要なディレクトリ構造と役割は以下の通りです。
（必要に応じて適宜読み替えてください）

* `/` (ルートディレクトリ): `index.html`, `env.js`, `chunk-widget.js`, `packaging.js` など、エントリーポイントや共通スクリプトを配置。
* `/game/`: ゲーム本体に関連するリソースやスクリプトを配置。
* `/ez1` 〜 `/ez6` (個別プロジェクト): 個別の機能検証や別バージョンの実装が配置されているディレクトリ。
  * **アクティブなプロジェクト**: `/ez1`, `/ez2`, `/ez5` のみ。
  * **除外対象**: 上記以外のアクティブではないナンバリングディレクトリ（`/ez3`, `/ez4`, `/ez6` 等）については、コードの把握や変更を一切行わず、処理全般をスキップしてください。特定の指示がない限り、ルートまたはアクティブなプロジェクトのディレクトリ内で作業を行ってください。ただし、非アクティブなプロジェクトが呼び出している `/`（ルート）内の共通ファイル（例: `env.js`, `chunk-widget.js`, `packaging.js` など）については、全体の構成理解のためにコードの把握対象に含めてください。
* `/log/`: ログ関連のファイルを格納。
* `.git/`, `.ignore`: Git等のバージョン管理用。

## 4. コーディング規約・エージェント向けルール (Coding Agent Guidelines)
コーディングエージェントは以下のルールを厳守してください。

1. **ツール利用の最適化**:
   * ファイル内容の確認には `view_file` ツールを使用し、bash/powershell上で `cat` コマンドを使用しないでください。
   * ファイルの検索には `grep_search` ツールを使用してください。
2. **ファイル編集の安全性**:
   * コードの編集には専用のツール（`replace_file_content`, `multi_replace_file_content`, `write_to_file`）を使用し、`echo` や `sed` コマンドによるターミナル経由のファイル編集は避けてください。
3. **無断での環境変更禁止**:
   * 新たなnpmパッケージ等の外部依存を導入する場合は、ユーザーへの確認を事前に行うか、明示的な指示がある場合にのみ行ってください。
4. **禁止領域への書き込み**:
   * 作業中の一時ファイルやスクリプトは、指定がない限り `tmp` フォルダや `.gemini` フォルダ内（アーティファクトとして作成する場合を除く）に直接書き込まないでください。
5. **コードの完全性**:
   * 既存のコードやコメント（docstringsを含む）は、タスクに関係がない限り保持し、勝手に削除しないでください。
6. **デザイン・UIの品質**:
   * フロントエンドの修正時には、シンプルすぎるMVP（Minimum Viable Product）にとどまらず、魅力的でモダンなUI（マイクロインタラクション、適切な配色、タイポグラフィなど）を提供するよう努めてください。
7. **ファイルのエンコーディング管理**:
   * 文字化けを防ぐため、ファイルの読み書き等でPythonスクリプトを使用する場合は、必ず `encoding="utf-8"` を指定してUTF-8での処理を徹底してください。
